export const environment = {
  production: false,
  useLocalDb: true,   // true = mock local (sin Supabase), false = Supabase real
  supabaseUrl: 'YOUR_SUPABASE_URL',
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY',
};
